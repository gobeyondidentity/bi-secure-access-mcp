# Beyond Identity API Token (REQUIRED)
# Get this from Beyond Identity Admin Console → Access Control → API Access
BEARER_TOKEN_BEARERAUTH=your_api_token_here

# Transport mode: 'http' for SSE, 'stdio' for standard input/output
# Default: stdio
MCP_TRANSPORT=http

# Server port (only used in HTTP mode)
# Default: 3000
PORT=3000

# Allowed origins for CORS (comma-separated, only used in HTTP mode)
# Leave empty to allow all origins (*)
# Example: https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=

# Node environment
NODE_ENV=production
